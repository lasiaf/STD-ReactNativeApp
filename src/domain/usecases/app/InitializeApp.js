export default class InitializeApp {

  constructor(repository) {
    this.repository = repository;
  }

  async execute() {
    /*
    INI Contoh dari ChatGPT
    await this.repository.createDatabase();

    await this.repository.createTables();

    await this.repository.syncDataFromApi();
    */

    const data = 'Hi';
  }

}